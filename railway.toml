[build]
builder = "nixpacks"

[deploy]
startCommand = "java -jar backend/target/smart-finance-coach-1.0.0.jar"
healthcheckPath = "/actuator/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
